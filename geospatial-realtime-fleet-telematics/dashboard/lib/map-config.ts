/** Mapbox style URL */
export const MAP_STYLE = "mapbox://styles/mapbox/dark-v11";
/** ศูนย์กลางแผนที่ (Bangkok) [lng, lat] */
export const MAP_CENTER: [number, number] = [100.52, 13.75];
/** ระดับ zoom เริ่มต้น */
export const MAP_ZOOM = 12;

/** คลาสสำหรับ container ของแผนที่ */
export const MAP_CONTAINER_CLASS = "h-full w-full";
/** คลาสสำหรับ wrapper รอบแผนที่ (รองรับ overlay) */
export const MAP_WRAPPER_CLASS = "relative h-full min-h-[400px] w-full";
