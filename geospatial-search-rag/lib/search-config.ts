/** ค่า default สำหรับการค้นหา */
export const DEFAULT_RADIUS_METERS = 5000;
export const DEFAULT_MATCH_LIMIT = 5;

/** ค่า similarity ขั้นต่ำที่ถือว่า match (0–1) */
export const MIN_SIMILARITY_THRESHOLD = 0.5;
