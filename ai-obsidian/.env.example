GOOGLE_API_KEY=
GEMINI_MODEL=gemini-2.5-flash-lite
NEXT_PUBLIC_API_BASE=http://localhost:4000

# glob สำหรับค้นหาไฟล์ใน vault ตอน ingest (แพ็กเกจ glob) — ค่าเริ่มต้นถ้าไม่ตั้ง: **/*.md
# ตัวอย่างรวม .txt: **/*.{md,txt}
# VAULT_GLOB=**/*.{md,txt}
